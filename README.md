# Medallion-Pipeline
This project implements a simple Medallion Architecture pipeline in Databricks using a ServiceNow incidents CSV dataset (`servicenow_incidents_10k.csv`).   Goal: show how I ingest raw data, clean/transform it, and produce an aggregated Gold dataset for analytics.
